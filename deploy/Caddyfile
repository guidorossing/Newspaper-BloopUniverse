# Caddy reverse proxy: automatische HTTPS voor het CMS.
# Vervang cms.rossingtm.com door je eigen (sub)domein dat via een
# A-record naar het IP van je VPS wijst.
cms.rossingtm.com {
	reverse_proxy localhost:3000 {
		header_up X-Forwarded-Proto {scheme}
	}
}
