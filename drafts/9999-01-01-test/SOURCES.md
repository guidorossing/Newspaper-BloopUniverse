# Test branch — not a real edition

This branch exists only to verify that the `edition-pr.yml` GitHub Action
can now open pull requests automatically, after the repo's "Allow GitHub
Actions to create and approve pull requests" setting was enabled. Safe to
close the resulting PR and delete this branch/folder immediately.
